export default function DashboardPage() {
  return (
    <main style={{ padding: 24, fontFamily: 'sans-serif' }}>
      <h1>Dashboard de mails</h1>
      <p>
        Próximo paso: consumir <code>/api/dashboard</code> acá y renderizar la
        tabla de registros con filtros por fecha y estado.
      </p>
    </main>
  );
}
